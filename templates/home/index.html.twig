{% extends 'base.html.twig' %}

{% block title %}Sygma{% endblock %}

{% block body %}


    <!-- Catégories nav -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">

            {% for category in categories %}

                <li class="nav-item">
                    <a class="nav-link" href="#">{{ category.name }}</a>    
                </li>

            {% endfor %}
      
            </ul>
        </div>
    </nav>



<div class="container">

    {% for service in services %}

        <div class="card" style="width: 18rem;">
        <img src="..." class="card-img-top" alt="...">
        <div class="card-body">
            <h5 class="card-title">{{ service.title }}</h5>
            <p class="card-text">{{ service.description }}</p>
            <h5>{{ service.price }}</h5> 
            <a href="{{ path('service_show', {'id': service.id}) }}" class="btn btn-primary">view product</a>
        </div>
        </div>

    {% endfor %}
    <br/> <br/> <br/>
    <h3> To do : </h3>
    
    
    <ul>
    <li> Revoir l'inscription ( bug ligne 44 security controller ) </li>
    <li> Tester la connexion </li>
    <li> Affichage des services par carte effectué </li>
    <li> Premier formulaire de création de service effectué </li>
    </ul>
   

</div>
{% endblock %}
